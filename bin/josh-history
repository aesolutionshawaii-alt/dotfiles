#!/bin/bash
# Scrape Claude history for content ideas
# Usage: josh-history [--days=7] [--export]

PROJECT_DIR="$HOME/Desktop/josh-x"

if [ ! -d "$PROJECT_DIR" ]; then
  echo "Error: Project not found at $PROJECT_DIR"
  exit 1
fi

cd "$PROJECT_DIR" && node src/scrape-claude-history.js "$@"
