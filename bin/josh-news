#!/bin/bash
# Scrape Bitcoin/AI news for Josh X content ideas
# Usage: josh-news [--source=bitcoin|ai|reddit]

PROJECT_DIR="$HOME/Desktop/josh-x"

if [ ! -d "$PROJECT_DIR" ]; then
  echo "Error: Project not found at $PROJECT_DIR"
  exit 1
fi

cd "$PROJECT_DIR" && node src/scrape-news.js "$@"
