#!/bin/bash
# Find reply opportunities for IBSH X account
# Usage: ibsh-replies [--url="tweet_url"]

PROJECT_DIR="$HOME/Desktop/ibsh-content-system"

if [ ! -d "$PROJECT_DIR" ]; then
  echo "Error: Project not found at $PROJECT_DIR"
  exit 1
fi

cd "$PROJECT_DIR" && node src/find-replies.js "$@"
