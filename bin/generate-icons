#!/bin/bash
# Generate PWA icons from SVG (192, 512, 180, 32px)

PROJECT_DIR="$HOME/Desktop/mango"

if [ ! -d "$PROJECT_DIR" ]; then
  echo "Error: Project not found at $PROJECT_DIR"
  exit 1
fi

cd "$PROJECT_DIR" && node generate-icons.js "$@"
