# OS files
.DS_Store
*.swp
*.swo

# Backups
*.backup

# Local overrides (create these for machine-specific config)
local.zsh

# Dependencies (reinstall with: cd ~/.dotfiles && npm install)
node_modules/
package-lock.json
